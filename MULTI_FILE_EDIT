// app/athletes/[id]/page.tsx
import React from 'react';
import AthletePageClient from '@/components/AthletePageClient';

const AthletePage = ({ params }) => {
  return (
    <div>
      {/* Public profile UI updates */}
      <AthletePageClient athleteId={params.id} />
    </div>
  );
};

export default AthletePage;

// app/athletes/[id]/AthletePageClient.tsx
import React from 'react';
import { Button } from 'shadcn/ui';
import PublicProfileActions from '@/components/public-profile-actions';

const AthletePageClient = ({ athleteId }) => {
  // Fetch athlete profile data here
  const profile = {
    verified: true,
    // other profile data
  };

  return (
    <div>
      {/* Public profile UI updates */}
      {!profile.verified && <div>Profile Verification Section Placeholder</div>}
      <PublicProfileActions athleteId={athleteId} />
    </div>
  );
};

export default AthletePageClient;

// components/public-profile-actions.tsx
import React from 'react';
import { Button } from 'shadcn/ui';

const PublicProfileActions = ({ athleteId }) => {
  return (
    <div>
      {/* Make the "Request Edit" button red */}
      <Button variant="destructive" className="bg-red-600 hover:bg-red-700 text-white" href={`/athletes/${athleteId}/edit-request`}>
        Request Edit
      </Button>
      {/* Other public profile actions */}
    </div>
  );
};

export default PublicProfileActions;
