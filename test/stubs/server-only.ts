/**
 * Test stub for the `server-only` package, which throws on import outside a React Server
 * Component. Aliased in vitest.config.ts so server libs stay unit-testable; the real
 * package is still what the Next build resolves.
 */
export {}
