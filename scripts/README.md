# Scripts

This folder has **many** one-off SQL and data scripts. Searching here is painful.

## Supabase SQL you’re likely to need

| Topic | File |
|--------|------|
| Fundraising hub: optional `fundraising_campaigns` table | `fundraising-campaigns-hub.sql` |
| Spartan donation mirror table | `add-spartan-donations.sql` |
| Spartan fundraising roster | `spartan-fundraising-athletes-seed.sql` |

**Convention:** When the AI gives you SQL to run, it should paste the **full script in the chat** so you can copy it into **Supabase → SQL Editor** without opening the repo. Use the paths above only as a backup.

## Deeper SQL subfolder

Some NHSCA / roster scripts also live under `scripts/sql/` and `scripts/data/`.
