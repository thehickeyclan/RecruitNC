-- Tournament of Champions — invitation SMS reminder tracking
-- Run in Supabase SQL Editor after toc-phase-2-invitations.sql.txt

alter table public.toc_invitations
  add column if not exists last_reminder_at timestamptz,
  add column if not exists last_reminder_body text;
