---
description: Data Dawg athlete answers lead with linked name + college commit summary
globs: lib/data-dawg*/**;lib/athlete-profile-links.ts;app/api/ai/**;lib/recruitnc-data-dawg-postprocess.ts
alwaysApply: true
---

# Data Dawg — athlete summary on top

For **any Data Dawg answer about a specific athlete** (search, “who is…”, “tell me about…”, dossier):

1. **Top of answer** = linked name via `Here's what I found about [Name](profile_url):` then an **analyst lead paragraph** (dossier uses `formatAnalystAthleteOpening` / `buildAnalystLeadParagraph` in `lib/data-dawg-athlete-analyst-profile.ts`; lighter surfaces may still use `formatAthleteAnswerOpening` in `lib/athlete-profile-links.ts`).
2. **Name is the profile hyperlink** — Use `getAthleteProfileUrl(id)` → `https://app.ncwrestlingunited.com/view-profile?id=…` (not `/unified-profile/[id]` and not `/athletes/[id]`). In chat HTML, open those links **same-tab** (never `target="_blank"` on app profile routes).
3. Include **career record**, **High School**, **Class of**, and **College commit** (with division when known) early — dossier: Career snapshot under the lead. **Do not** put **Weight** in the header — it changes often; weight belongs on tournament result lines only.
4. Prefer **Hall-of-Fame storytelling**: fact-driven lead (verified titles/AAs/awards/rank — not generic “one of the top…”) → visual Career snapshot → Development path (HS / club / **NC United** Blue + National Team bullets / college) → Career progression (season W–L under each year) → Historical context (media-guide paragraph) → Notable achievements → Historical rankings → **Represented North Carolina** (above detailed State results) → state / national résumés → closer → verified sources footer. Timeline labels should show progression (First / Returned to podium / Broke through / Repeated), not four identical “State Champion” lines. Super32 “Best listed result” must use best W–L or best placement — never the first chronological row.
5. **No markdown headings** (`###` / `##` / `#`). Plain section labels only (e.g. `Career progression:`, `Historical context:`). Postprocess strips leading `#` markers.
6. Do not invent a profile URL without a directory id; tournament-only alumni can use a plain name. Historical context and notable bullets must be **derived from verified data** (title counts, placers arc, record books, rankings, dual title years, awards, NC United results, club field when present) — never invent school prestige without dual-title evidence. College transfers: `College career: UNC → NC State` / snapshot `UNC → NC State` (original first).
